################################################################################
# Defines the provider (AWS)
################################################################################

provider "aws" {
  region = local.region
}
